$(() => {
  $('time.timeago').timeago();
});

const initTimeAgo = function($elements) {
  // $.each($elements, (index, $element) => {
  //   $elements[index].text('hello');
  //   // const isoDate = new Date($element.text()).toISOString();
  //   // $element.prop('datetime', isoDate);
  //   // $element.text('hello');
  // })
  // $elements.timeago();
}
